import React from 'react';
import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useNavigation } from '@react-navigation/native';

const tools = [
  { name: 'Chat', icon: 'chatbubbles', screen: 'Chat', params: {} },
  { name: 'Pin Creator', icon: 'pin', screen: 'Content', params: { tool: 'pin' } },
  { name: 'Ad Copy', icon: 'megaphone', screen: 'Content', params: { tool: 'ad' } },
  { name: 'Video Script', icon: 'videocam', screen: 'Content', params: { tool: 'script' } },
  { name: 'Proposal', icon: 'document-text', screen: 'Content', params: { tool: 'proposal' } },
  { name: 'Email Drafter', icon: 'mail', screen: 'Comms', params: { tool: 'email' } },
  { name: 'Comment Reply', icon: 'chatbox', screen: 'Comms', params: { tool: 'comment' } },
  { name: 'Trend Analyzer', icon: 'trending-up', screen: 'Intel', params: { tool: 'trend' } },
  { name: 'Competitor', icon: 'people', screen: 'Intel', params: { tool: 'competitor' } },
  { name: 'Post Scheduler', icon: 'time', screen: 'Schedule', params: { tool: 'schedule' } },
  { name: 'Digistore24', icon: 'cart', screen: 'Store', params: {} },
];

export default function HomeScreen() {
  const navigation = useNavigation<any>();
  return (
    <View style={styles.container}>
      <Text style={styles.greeting}>Jarvis</Text>
      <Text style={styles.subtitle}>Human-in-the-loop AI Assistant</Text>
      <ScrollView contentContainerStyle={styles.grid}>
        {tools.map((tool, index) => (
          <TouchableOpacity
            key={index}
            style={styles.card}
            onPress={() => navigation.navigate(tool.screen, tool.params)}
          >
            <Ionicons name={tool.icon as any} size={32} color="#00E5FF" />
            <Text style={styles.cardText}>{tool.name}</Text>
          </TouchableOpacity>
        ))}
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#0A0A0A', paddingTop: 60, paddingHorizontal: 20 },
  greeting: { fontSize: 36, fontWeight: 'bold', color: '#00E5FF', textAlign: 'center' },
  subtitle: { fontSize: 14, color: '#777', textAlign: 'center', marginBottom: 30 },
  grid: { flexDirection: 'row', flexWrap: 'wrap', justifyContent: 'space-between' },
  card: {
    width: '30%', backgroundColor: '#1A1A2E', borderRadius: 16, paddingVertical: 20,
    alignItems: 'center', marginBottom: 15, borderWidth: 1, borderColor: '#2A2A3E',
  },
  cardText: { color: '#CCC', marginTop: 8, fontSize: 12, textAlign: 'center' },
});
